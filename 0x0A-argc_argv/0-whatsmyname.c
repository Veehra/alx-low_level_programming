#include <stdio.h>

/**
 * main - Entry point
 * @argc: number of arguments
 * @argv: number of arguments in an array
 *
 * Return: 0;
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
}
