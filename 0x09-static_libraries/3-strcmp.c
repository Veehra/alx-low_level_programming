#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * _strcmp - Entry point
 * @s1: pointer
 * @s2: pointer
 *
 * Return: 0
 */

int _strcmp(char *s1, char *s2)
{
	int i = strcmp(s1, s2);

	return (i);
}
