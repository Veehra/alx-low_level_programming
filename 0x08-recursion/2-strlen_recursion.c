#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * _strlen_recursion - Entry point
 * @s: pointer
 *
 * Return: void
 */

int _strlen_recursion(char *s)
{
	int i = strlen(s);

	return (i);
}
